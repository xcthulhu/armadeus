#
#

config ARMADEUS_FPGA_IRQ_MANAGER
        tristate "OpenCore IRQ manager driver"
        default n
        depends on ARMADEUS_FPGA_DRIVERS
        ---help---
	  You need it if you have a POD generated "IP"

