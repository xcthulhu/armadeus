diff -purN fbset/Makefile fbset-2.1/Makefile
--- fbset/Makefile	1999-01-17 20:15:46.000000000 +0100
+++ fbset-2.1/Makefile	2006-01-20 16:17:19.000000000 +0100
@@ -2,7 +2,7 @@
 # Linux Frame Buffer Device Configuration
 #
 
-CC =		gcc -Wall -O2 -I.
+CC =		$(CROSS_COMPILE)gcc -Wall -O2 -I.
 BISON =		bison -d
 FLEX =		flex
 INSTALL =	install
@@ -39,3 +39,6 @@ install:	fbset
 
 clean:
 		$(RM) *.o fbset lex.yy.c modes.tab.c modes.tab.h
+
+%.o:		%.c
+		$(CC) -c $(CFLAGS) -o $@ $<
