--- buildroot/package/mpfr/mpfr.mk.ori	2008-11-03 09:15:26.000000000 +0100
+++ buildroot/package/mpfr/mpfr.mk	2009-01-03 17:47:34.000000000 +0100
@@ -21,11 +21,13 @@
 MPFR_PATCH_SOURCE:=$(DL_DIR)/$(MPFR_PATCH_FILE)
 
 $(MPFR_PATCH_SOURCE):
-	$(WGET) -O $@ $(MPFR_SITE)/$(MPFR_PATCH)
+	-$(WGET) -O $@ $(MPFR_SITE)/$(MPFR_PATCH)
+	$(Q)test -e $@ || $(WGET) -O $@ $(ALTERNATE_SITE)/$(MPFR_PATCH)
 endif
 
 $(DL_DIR)/$(MPFR_SOURCE):
-	 $(WGET) -P $(DL_DIR) $(MPFR_SITE)/$(MPFR_SOURCE)
+	-$(WGET) -P $(DL_DIR) $(MPFR_SITE)/$(MPFR_SOURCE)
+	$(Q)test -e $@ || $(WGET) -O $@ $(BR2_ALTERNATE_SITE)/$(MPFR_SOURCE)
 
 $(MPFR_DIR)/.unpacked: $(DL_DIR)/$(MPFR_SOURCE) $(MPFR_PATCH_SOURCE)
 	$(MPFR_CAT) $(DL_DIR)/$(MPFR_SOURCE) | tar -C $(TOOL_BUILD_DIR) $(TAR_OPTIONS) -
