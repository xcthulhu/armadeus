--- buildroot.org/buildroot/package/sdl/Config.in	2006-09-08 09:15:13.000000000 +0200
+++ buildroot/package/sdl/Config.in	2006-11-29 12:14:45.000000000 +0100
@@ -5,3 +5,27 @@
 	help
 
 	  http://www.libsdl.org/
+
+config BR2_PACKAGE_SDL_IMAGE
+	bool "SDL_image"
+	depends on BR2_PACKAGE_SDL
+	default n
+	help
+
+	  SDL_image is an image file loading library. It loads images as SDL surfaces, 
+	  and supports the following formats: BMP, GIF, JPEG, LBM, PCX, PNG, PNM, TGA, TIFF, XCF, XPM, XV.
+
+	  http://www.libsdl.org/projects/SDL_image/
+	  
+
+config BR2_PACKAGE_SDL_TTF
+	bool "SDL_ttf"
+	depends on BR2_PACKAGE_SDL
+	select BR2_PACKAGE_FREETYPE
+	default n
+	help
+
+	  This is a sample library which allows you to use TrueType fonts in your SDL applications.
+
+	  http://www.libsdl.org/projects/SDL_ttf/
+	  
