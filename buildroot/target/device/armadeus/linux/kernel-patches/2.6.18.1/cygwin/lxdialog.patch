--- linux-2.6.18.1/scripts/kconfig/lxdialog/Makefile	2006-10-14 05:34:03.000000000 +0200
+++ linux-2.6.18.1/scripts/kconfig/lxdialog/Makefile	2006-11-09 20:01:27.458793600 +0100
@@ -6,7 +6,7 @@ check-lxdialog  := $(srctree)/$(src)/che
 # Use reursively expanded variables so we do not call gcc unless
 # we really need to do so. (Do not call gcc as part of make mrproper)
 HOST_EXTRACFLAGS = $(shell $(CONFIG_SHELL) $(check-lxdialog) -ccflags)
-HOST_LOADLIBES   = $(shell $(CONFIG_SHELL) $(check-lxdialog) -ldflags $(HOSTCC))
+HOST_LOADLIBES   = $(shell $(CONFIG_SHELL) $(check-lxdialog) -ldflags $(HOSTCC)) -lcurses
 
 HOST_EXTRACFLAGS += -DLOCALE
 
