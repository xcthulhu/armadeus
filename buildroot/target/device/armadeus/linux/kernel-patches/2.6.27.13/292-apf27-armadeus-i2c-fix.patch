--- linux-2.6.27.2ref/drivers/i2c/busses/i2c-mxc.c	2008-11-25 14:52:25.000000000 +0100
+++ linux-2.6.27.2/drivers/i2c/busses/i2c-mxc.c	2008-11-29 22:07:55.000000000 +0100
@@ -115,7 +115,7 @@ static void imx_i2c_stop(imx_i2c_device 
  */
 static int imx_i2c_wait_for_tc(imx_i2c_device * dev, int trans_flag)
 {
-	int retry = 16;
+	int retry = 4;
 
 	while (retry-- && !transfer_done) {
 		wait_event_interruptible_timeout(dev->wq,
@@ -125,16 +125,14 @@ static int imx_i2c_wait_for_tc(imx_i2c_d
 	transfer_done = false;
 
 	if (retry <= 0) {
+        if (!tx_success) {
+    			/* An ACK was not received */
+    			printk(KERN_DEBUG "ACK not received \n");
+    	}
 		/* Unable to send data */
 		printk(KERN_DEBUG "Data not transmitted\n");
 		return -1;
-	} else if (!(trans_flag & I2C_M_RD)) {
-		if (!tx_success) {
-			/* An ACK was not received for transmitted byte */
-			printk(KERN_DEBUG "ACK not received \n");
-			return -1;
-		}
-	}
+	} 
 
 	return 0;
 }
@@ -441,10 +439,16 @@ static irqreturn_t imx_i2c_handler(int i
 		/* Interrupt due byte transfer completion */
 		tx_success = false;
 		/* Check if RXAK is received in Transmit mode */
-		if ((cr & MXC_I2CR_MTX) && (!(sr & MXC_I2SR_RXAK))) {
-			tx_success = true;
+		if (cr & MXC_I2CR_MTX) {
+            if( !(sr & MXC_I2SR_RXAK)) {
+			    tx_success = true;
+    		    transfer_done = true;
+            }
 		}
-		transfer_done = true;
+        else {
+    		tx_success = true;
+            transfer_done = true;
+        }
 		wake_up_interruptible(&dev->wq);
 	}
 
