config BR2_PACKAGE_IMXREGS
	bool "imxregs"
	default n
	help
	  This debug tool allows you to access the i.MX internal registers 
	  directly from Linux userspace. (If operation was allowed in U-Boot).

